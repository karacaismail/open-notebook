import { SettingsWorkspace } from '@/components/settings/SettingsWorkspace'
/** Preserve saved links while using the same settings workspace. */
export default function ModulesPage() { return <SettingsWorkspace initialTab="modules" /> }
